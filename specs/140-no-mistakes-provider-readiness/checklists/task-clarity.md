# Task Clarity Checklist: No-Mistakes Provider Readiness

**Purpose**: Validate that the requirements and task list clearly cover the current reviewer-failure problem set before further implementation.
**Created**: 2026-05-18
**Feature**: `specs/140-no-mistakes-provider-readiness/spec.md`
**Audience/Timing**: PR reviewer before implementation planning refresh
**Depth**: Standard

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements. The prerequisite script rejected `main`, so this uses the active feature declared in `AGENTS.md`: `specs/140-no-mistakes-provider-readiness`.

## Requirement Completeness

- [x] CHK001 Are the current observed failure cases enumerated as requirements inputs, including Claude/Gemini/Kimi CLI auth/readiness, Grok transport readiness, DeepSeek/GLM direct API provider availability, result lookup, parser quality, and cancellation states? [Gap, Plan §2026-05-18 dogfood failures, Spec §User Story 1]
- [x] CHK002 Are provider transport modes specified per provider so CLI/subscription, local tunnel, and direct API paths cannot be conflated? [Gap, Spec §FR-001, Plan §Scale/Scope]
- [x] CHK003 Are requirements defined for the global rule that defaults/docs must not silently select `--auth-mode auto` or change auth path, provider, transport, or billing surface? [Gap, Spec §Requirements]
- [x] CHK004 Are Grok CLI subscription requirements documented as a first-class transport distinct from the legacy Grok web tunnel path? [Gap, Spec §User Story 2, Plan §Implementation Slices]
- [x] CHK005 Are legacy Grok tunnel/grok2api requirements marked as current, fallback, or deprecated so future tasks cannot implement both ambiguously? [Ambiguity, Spec §FR-003-FR-005, Plan §Implementation Slices]
- [x] CHK006 Are source-free preflight requirements defined for every source-bearing provider path, not only direct API and Grok tunnel checks? [Gap, Spec §FR-002, Spec §SC-003]
- [x] CHK007 Are same-path repair requirements specified separately from forbidden fallback behavior? [Gap, Spec §SC-005]
- [x] CHK008 Are cancellation and stuck-running requirements complete for foreground and background reviewer jobs across all continuation-capable providers? [Completeness, Plan §Source-bearing failure and abort states] Evidence: T057 maps Claude/Gemini/Kimi cancel and stuck-running smoke coverage; Grok has no cancel command.

## Requirement Clarity

- [x] CHK009 Is "magically works" translated into precise requirements such as deterministic preflight, safe same-transport retry, explicit unavailable classification, and no surprise source-send state? [Ambiguity, User request, Spec §SC-005]
- [x] CHK010 Is "provider unavailable" clarified into transport-specific subclasses where needed, such as CLI missing, CLI unauthenticated, tunnel unavailable, session token missing, API provider unavailable, and review-quality failure? [Clarity, Spec §SC-005]
- [x] CHK011 Is "no silent fallback" defined with explicit allowed and forbidden examples, including that operator-selected `--auth-mode auto` fallback is allowed but defaulting to auto is not? [Gap, Spec §Requirements]
- [x] CHK012 Is "safe repair" defined with objective boundaries, including which repairs are automatic, which require operator approval, and which must stop before source send? [Ambiguity, Plan §Source-bearing failure and abort states]
- [x] CHK013 Is the relationship between source-free doctor success and source-bearing review readiness stated clearly enough to prevent stale doctor evidence from authorizing source send? [Clarity, Plan §Source-bearing failure and abort states]
- [x] CHK014 Are terms such as `review-ready`, `approve`, `completed`, `failed_review_slot`, and `source_content_transmission` defined so they cannot be treated as interchangeable? [Clarity, Spec §FR-002, Spec §FR-008, Plan §Review-quality failure surfacing]

## Requirement Consistency

- [x] CHK015 Do Grok requirements align with the proposed CLI subscription path rather than continuing to assume tunnel/grok2api is the only subscription-backed transport? [Conflict, Spec §User Story 2, Plan §Implementation Slices]
- [x] CHK016 Do direct API approval requirements align with the new requirement that source-free provider availability must be checked immediately before source-bearing send? [Consistency, Spec §FR-006, Plan §Source-bearing failure and abort states]
- [x] CHK017 Do task-list phases align with the expanded failure classes, or do completed tasks imply the feature is done while current requirements remain uncovered? [Conflict, Tasks §Phase 1-4, Plan §2026-05-18 dogfood failures]
- [x] CHK018 Are generated command/skill docs required to preserve explicit auth/transport selection instead of defaulting to convenience flags such as `--auth-mode auto`? [Consistency, Plan §Contract hardening]
- [x] CHK019 Are result lookup, review panel, readiness manifest, and lifecycle card requirements consistent about workspace identity and retrieval commands? [Consistency, Plan §Result command normalization, Plan §Workspace-aware result lookup diagnostics]

## Acceptance Criteria Quality

- [x] CHK020 Are success criteria measurable for proving no source-bearing payload is sent when a same-path preflight fails? [Acceptance Criteria, Spec §SC-003, Spec §SC-004]
- [x] CHK021 Are success criteria defined for each provider having an explicit terminal state when unavailable, rather than leaving a slot pending or prose-only? [Gap, Spec §SC-001, Spec §SC-005]
- [x] CHK022 Are acceptance criteria defined for Grok CLI readiness, headless review, auth failure, timeout, result parsing, source-bearing audit fields, neutral cwd, and logging/cache/auth privacy if Grok CLI becomes the default Grok transport? [Gap, Spec §Success Criteria] Evidence: T053 contract accepted by V3 external review; T034 implements and verifies the wrapper with source/cached smoke, real source-free doctor, cached dummy source-bearing run, temp `GROK_HOME`, and cleanup proof.
- [x] CHK023 Are criteria measurable for distinguishing retryable same-path failures from forbidden fallback attempts? [Gap, Spec §SC-005]
- [x] CHK024 Are criteria defined for stale-evidence prevention, including current-run preflight timestamp or immediate-pre-send proof? [Gap, Plan §Source-bearing failure and abort states]
- [x] CHK025 Can completion be objectively determined from tasks, tests, and manifest rows without relying on chat summaries or lifecycle-only output? [Measurability, Spec §SC-001, Plan §Operator aggregation path]

## Scenario Coverage

- [x] CHK026 Are primary scenarios covered for each provider family: companion CLI, Grok CLI, legacy Grok tunnel if retained, and direct API? [Coverage, Spec §User Story 1]
- [x] CHK027 Are exception scenarios covered for CLI not installed, CLI unauthenticated, OAuth rejected, tunnel unavailable, missing session tokens, API outage, provider prompt-size cap, rate limit/quota, timeout, parser failure, shallow/missing-verdict output, partial source transmission, and prompt sidecar leakage? [Coverage, Spec §SC-005]
- [x] CHK028 Are recovery scenarios defined for auth refresh, tunnel restart, Grok CLI login, safe retry, result retrieval from launch workspace, and explicit provider waiver? [Gap, Plan §Workspace-aware result lookup diagnostics, Plan §Source-bearing failure and abort states]
- [x] CHK029 Are continuation scenarios specified for all providers that support session resume, and are unsupported continuation cases intentionally excluded? [Coverage, Spec §User Story 4, Spec §FR-010-FR-012]
- [x] CHK030 Are no-mutations and approval-gate scenarios specified for any repair path that could touch browser sessions, credentials, installed cache, GitHub, or local provider state? [Coverage, Spec §FR-016]

## Dependencies & Assumptions

- [x] CHK031 Are assumptions documented for which Grok CLI version, auth mode, headless output format, and subscription tier are required? [Gap, Grok CLI transport assumption] Evidence: T034 source/cached doctors observed `grok 0.1.211 (2f2cd6d5c2)`, grok.com login, default model `grok-build`, headless `--prompt-file` and `--output-format json`; CLI exposes no explicit tier field, so wrapper records version/model/auth evidence and fails closed if the model is unavailable.
- [x] CHK032 Are assumptions documented for installed plugin cache sync versus source tree behavior when command docs, skills, copied libraries, and runtime scripts diverge? [Assumption, Plan §Verification package] Evidence: T032 cache/source audit closed current drift; T044 remains mandatory after generated docs, skills, copied libs, or installed-cache-sensitive runtime changes.
- [x] CHK033 Are external service nondeterminism assumptions documented so provider outages are treated as unavailable states, not implementation failures or approvals? [Assumption, Spec §Assumptions]
- [x] CHK034 Are privacy assumptions documented for prompt sidecars, selected source, stored JobRecords, and result manifests across every transport? [Dependencies, Spec §FR-009, Plan §Contract hardening]

## Task List Quality

- [x] CHK035 Does `tasks.md` contain uncompleted tasks for every currently known gap, rather than only completed tasks for the older scope? [Gap, Tasks §Phase 1-4]
- [x] CHK036 Are tasks grouped by independently verifiable failure class so work can proceed without mixing parser, transport, auth, source-send, and cancellation fixes? [Gap, Tasks §Phase 1-4]
- [x] CHK037 Does each task identify the provider family and affected contract surface, such as runtime script, shared library, command doc, skill doc, smoke test, fixture, or manifest schema? [Gap, Tasks §Phase 1-4]
- [x] CHK038 Are task dependencies clear between spec update, transport design, test updates, implementation, fixture refresh, installed-cache sync, and live smoke evidence? [Gap, Tasks §Phase 4]
- [x] CHK039 Are explicit non-goals captured so tasks do not add default API fallback, cross-provider fallback, billing changes, or unapproved browser/session mutation while preserving explicit `--auth-mode auto` behavior? [Gap, Tasks §Phase 1-4]
- [x] CHK040 Can a reviewer map every task back to a requirement or success criterion without guessing from chat context? [Traceability, Tasks §All, Spec §Requirements]
- [x] CHK041 Does Grok CLI classify a launched-but-failed source-bearing CLI process as `may_be_sent`, not `sent` or `not_sent`, unless a clean response proves delivery? [Gap, Claude latest dirty-diff review B1, Quickstart §Source-send truth]
- [x] CHK042 Is Grok CLI default proof tied to a current real source-bearing CLI job that accepted the canonical flag set, not only source-free doctor output or mock tests? [Evidence, Claude latest dirty-diff review B2, Evidence ledger §Latest Dirty-Diff External Review]
- [x] CHK043 Does Grok CLI keep compound privacy-persistence plus nonzero source-bearing failure classified as `may_be_sent`, not `sent`? [Gap, Claude focused re-review residual 1, Quickstart §Source-send truth]
- [x] CHK044 Does Grok CLI prove the missing-binary/prelaunch path remains `not_sent`? [Gap, Claude focused re-review residual 3, Quickstart §Pre-Send Gate]
- [x] CHK045 Do Grok CLI privacy-persistence failures avoid tunnel repair guidance and keep test-created temp-home residue cleaned up? [Gap, Claude focused re-review nonblocking concern, Quickstart §Privacy Boundary]
- [x] CHK046 Does Grok CLI fail closed as `privacy_persistence` when a source-bearing prompt sidecar cleanup is not fully verified? [Gap, Claude final focused re-review NB2, Quickstart §Privacy Boundary]
- [x] CHK047 Does Grok CLI JobRecord expose source-free preflight `GROK_HOME` cleanup separately from source-bearing `GROK_HOME` cleanup? [Gap, Claude final focused re-review NB3, Evidence ledger §T063/T064]
- [x] CHK048 Does Grok CLI scrub direct API env vars (`GROK_API_KEY`, `XAI_API_KEY`, `XAI_KEY`) from subscription CLI subprocesses? [Gap, Claude final focused re-review NB2, Spec §FR-001]
- [x] CHK049 Does a failed source-free Grok CLI preflight label cleanup diagnostics as source-free rather than source-bearing? [Gap, Claude final focused re-review NB1, Evidence ledger §T065-T068]
- [x] CHK050 Does compound Grok CLI privacy-persistence retain redacted CLI exit/stderr diagnostics for operator repair? [Gap, Claude final focused re-review NB3, Quickstart §Recovery Policy]
- [x] CHK051 Does the happy-path Grok CLI JobRecord assert both source-free prompt cleanup and source-free home cleanup fields? [Gap, Claude final focused re-review NB4, Evidence ledger §T065-T068]
